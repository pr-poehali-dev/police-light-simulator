# 📱 Инструкция по сборке Android APK

## Что уже сделано ✅

- ✅ Установлен Capacitor для Android
- ✅ Настроена конфигурация приложения
- ✅ Добавлена поддержка вспышки камеры
- ✅ Приложение готово к сборке

## Требования

Для сборки APK вам понадобится:

1. **Node.js** (версия 18 или выше) - [скачать](https://nodejs.org/)
2. **Android Studio** - [скачать](https://developer.android.com/studio)
3. **Java Development Kit (JDK)** версия 17 - обычно идет с Android Studio

## Пошаговая инструкция

### Шаг 1: Скачать проект

Скачайте код проекта с GitHub (используйте интеграцию с GitHub в poehali.dev).

### Шаг 2: Установить зависимости

Откройте терминал в папке проекта и выполните:

```bash
npm install
```

### Шаг 3: Собрать веб-версию

```bash
npm run build
```

### Шаг 4: Инициализировать Android проект

Выполните команду (только первый раз):

```bash
npx cap add android
```

### Шаг 5: Синхронизировать с Android

```bash
npx cap sync android
```

### Шаг 6: Открыть проект в Android Studio

```bash
npx cap open android
```

Это откроет проект в Android Studio.

### Шаг 7: Настроить права доступа

В Android Studio откройте файл:
```
android/app/src/main/AndroidManifest.xml
```

Добавьте разрешения для использования камеры и вспышки (если их нет):

```xml
<uses-permission android:name="android.permission.CAMERA" />
<uses-feature android:name="android.hardware.camera" />
<uses-feature android:name="android.hardware.camera.flash" />
```

### Шаг 8: Собрать APK

В Android Studio:

1. Нажмите **Build → Build Bundle(s) / APK(s) → Build APK(s)**
2. Дождитесь завершения сборки
3. Нажмите на уведомление **locate** чтобы найти APK файл

APK будет находиться в:
```
android/app/build/outputs/apk/debug/app-debug.apk
```

### Альтернативный способ (через командную строку)

В папке проекта выполните:

```bash
cd android
./gradlew assembleDebug
```

APK появится в той же папке: `android/app/build/outputs/apk/debug/app-debug.apk`

## Установка APK на телефон

### Способ 1: Через USB
1. Включите **Отладку по USB** в настройках разработчика на телефоне
2. Подключите телефон к компьютеру
3. В Android Studio нажмите **Run** (зеленая кнопка Play)

### Способ 2: Вручную
1. Скопируйте файл `app-debug.apk` на телефон
2. Откройте файл на телефоне
3. Разрешите установку из неизвестных источников
4. Установите приложение

## Возможные проблемы

### "SDK not found"
Установите Android SDK через Android Studio: **Tools → SDK Manager**

### "JDK not found"
Установите JDK 17 и настройте переменную окружения `JAVA_HOME`

### Ошибки сборки Gradle
Попробуйте очистить кеш:
```bash
cd android
./gradlew clean
```

## Создание релизной версии (для Google Play)

Для публикации в Google Play Store нужно:

1. Создать ключ подписи (keystore)
2. Настроить `android/app/build.gradle`
3. Собрать релизный APK: `./gradlew assembleRelease`
4. Загрузить в Google Play Console

Подробная инструкция: https://capacitorjs.com/docs/android/deploying-to-google-play

## Полезные команды

```bash
# Пересобрать и синхронизировать
npm run build && npx cap sync android

# Открыть Android Studio
npx cap open android

# Обновить Capacitor
npm install @capacitor/core@latest @capacitor/cli@latest @capacitor/android@latest

# Посмотреть логи приложения
npx cap run android -l
```

## Информация о приложении

- **Название**: Мигалка
- **Package ID**: dev.poehali.flashlight
- **Версия**: 1.0.0

## Что дальше?

После установки приложение будет:
- ✅ Работать офлайн
- ✅ Использовать реальную вспышку камеры (режим "Фонарик")
- ✅ Показывать цветные мигания на экране (режим "Экран")
- ✅ Иметь настройки цвета и скорости

Удачи! 🚀
